spring:
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: icore-config-server
  application:
    name: config-client-test
server:
  port: 18100
eureka:
  client:
    serviceUrl:
      defaultZone: http://192.168.31.116:18080/eureka/
